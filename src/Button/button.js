import React from 'react'

const Button = ({name}) => {
  return (
    <div className=''>
    <a className="f6 link dim br3 ph4 pv3 mb2 dib white bg-blue" href="#0">{name}</a>
    </div>
  )
}

export default Button